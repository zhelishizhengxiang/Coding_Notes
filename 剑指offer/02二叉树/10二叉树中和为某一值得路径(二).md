[二叉树中和为某一值的路径(二)_牛客题霸_牛客网](https://www.nowcoder.com/practice/b736e784e3e34731af99065031301bca?tpId=13&tqId=23276&sourceUrl=%2Fexam%2Foj%2Fta%3Fpage%3D1%26tpId%3D13%26type%3D13)

此题与[14路径总和2](../../代码随想录/06二叉树/14路径总和2.md)是同一道题
```java
import java.util.*;

/*
 * public class TreeNode {
 *   int val = 0;
 *   TreeNode left = null;
 *   TreeNode right = null;
 *   public TreeNode(int val) {
 *     this.val = val;
 *   }
 * }
 */

public class Solution {
    List<Integer> path = new ArrayList<>();
    ArrayList<ArrayList<Integer>> res = new ArrayList<>();
    public void traversal(TreeNode root, int target) {
        if (root.left == null && root.right == null) {
            if (target == 0)
                res.add(new ArrayList<>(path));
            else
                return;
        }

        if (root.left != null) {
            path.add(root.left.val);
            traversal(root.left, target - root.left.val);
            path.remove(path.size() - 1);
        }

        if (root.right != null) {
            path.add(root.right.val);
            traversal(root.right, target - root.right.val);
            path.remove(path.size() - 1);
        }
    }
    /**
     * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
     *
     *
     * @param root TreeNode类
     * @param target int整型
     * @return int整型ArrayList<ArrayList<>>
     */
    public ArrayList<ArrayList<Integer>> FindPath (TreeNode root, int target) {
        // write code here
        if (root == null)
            return res;
        path.add(root.val);
        traversal(root, target-root.val);
        return res;

    }
}
```